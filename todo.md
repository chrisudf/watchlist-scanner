# todo.md — 未决项

记**还没验完**的东西，以及每项"怎么才算可以关掉"。已经踩过并定论的坑
去 [lesson.md](lesson.md)。

---

## 1. 陈旧日线硬拦 —— 已实现，等生产数据验证

**状态**: 代码已上（日期比对 + ⏸ 硬拦），但**只在盘后 `--force` 的人造
条件下验证过**，生产上一次都还没触发过。

### 背景

2026-09-04 盘后跑全量扫描，11 个标的亮了 10 个假 RR 倒挂旗标。根因是
yfinance 日线最新一根返回 NaN，scanner 退回前一日收盘当 spot，用昨天的
股价去反解今天的期权报价（详见 lesson.md）。

修了两层：

1. RR 的 IV/delta 改用 put-call parity 反解的 forward，陈旧 spot 不再
   传导到旗标；
2. 陈旧日线本身做成硬拦 —— 因为它同时污染收盘价、右侧状态判定和所有
   票据，不只是 RR。

### 判据为什么是日期比对而不是价差

一开始用的是"forward 反解现货 vs 日线收盘"的价差（`rr_spot_gap_warn`）。
问题：**难借券的高借券费会让 forward 合法地低于现货好几个百分点** ——
借券费在定价上等同于股息，`F = S·e^((r−q−b)T)`，`b` 一大 F 就掉下来。
价差判据分不开"数据陈旧"和"借券贵"，拿它硬拦会把 RKLB / SOFI / HOOD /
SPCX 这类高做空兴趣的标的**永久静音**，而且给出的理由（"日线陈旧"）
还是错的。

改成比对**日线 bar 的日期** vs **期权链 `max(lastTradeDate)` 的日期**：
借券费再贵也不会让两个日期对不上。单向判断 —— 只有期权比日线新才算陈旧，
反过来（期权好几天没成交）是流动性问题不是数据问题。

### 待验证

- [ ] **生产上会不会触发**。理论上不该 —— 定时任务不带 `--force`，
      `market_is_live()`（SPY 近 20 分钟有没有 1m bar）会先 exit 3。
      但那道检查证明的是"整个市场在交易"，**不证明"这只票的日线是今天
      的"**，残余风险就在这里。留意邮件里有没有出现 ⏸ 日线陈旧。
- [ ] **会不会误伤**。特别看难借券的那几只（RKLB / SOFI / HOOD /
      SPCX）：它们应该走"持有成本异常"那条 note，**不该**被硬拦。
- [ ] **盘中真实运行确认**。至今所有验证都是盘后 `--force` 造出来的，
      没有一次是在美股开盘时段跑的。

### 关闭条件

连续 10 个交易日的定时报告里，① 没有非预期的 ⏸ 日线陈旧；② 难借券标的
仍能正常出票。满足则删掉这一节，把结论并入 lesson.md。

### 相关

`scanner.py` 的 `daily_bar_stale()` / `blocked_ticket()`；测试
`TestDailyBarStale`、`TestBlockedTicket`。

---

## 2. RKLB / GLD 的 RR 倒挂旗标 —— 真伪未定

**状态**: forward 修复后，假旗标从 10 个降到 2 个。剩下这两个**看起来
是真的**，但没有独立确认过。

```
RKLB  80C IV 69.9% > 56P IV 66.5%   RR −3.4 pts
GLD  435C IV 25.9% > 392P IV 24.3%  RR −1.6 pts
```

### 为什么还不能下结论

- 两个读数都**压在 `rr_invert_min_pts = 1.0` 的噪声地板附近**，GLD 的
  −1.6 尤其近。地板本身是按"延迟报价有 ~1 pt run-to-run 漂移"定的。
- 数据是**盘后**的。盘后价差天然变宽，虽然新的 `_rr_mark` 会拒掉过宽的
  报价，但通过过滤的那些报价质量仍不如盘中。
- 单点观测，没有时间序列 —— 分不清"持续的 call skew"和"当天的噪声"。

两个都有先验上的合理性（RKLB 是高波动太空股，追涨常态；黄金本来就常有
上行需求推出的 call skew），但先验不等于验证。

### 待验证

- [ ] 盘中跑一次（布里斯班 23:30 之后），看这两个旗标还在不在、数值稳
      不稳。
- [ ] 连续几天看是否持续 —— 真 skew 会持续，噪声不会。
- [ ] 和外部源交叉核对（moomoo 的合约 IV，或 volchart.io 的 skew）。

### 关闭条件

盘中运行 + 连续 3 天都在 → 认定为真信号，记进 lesson.md 当作"旗标正常
工作"的基线样本。若盘中消失 → 说明 1.0 pt 的地板对盘后数据不够，需要
按数据时段分别定阈值。

---

## 3. LEAP IV 档位 —— 已上线，生产未验证、阈值未校准

**状态**: PR #18 于 2026-09-25 合并并部署。LEAP 真票发出时按"平值 IV ÷ 自身
10 年实际波动中位数"分 A/B/C/D 档，C/D 改写成 🟡 spread 提示。来龙去脉见
lesson.md 2026-09-24 / 2026-09-25 两节。

### 待验证

- [ ] **生产报告显示正常**。第一份带新代码的收盘报告是 2026-09-26 05:45 AEST
      （美东 9/25 收盘）。LEAP 票要带档位 note 和 IV 来源标注；不该出现
      "IV 档位计算失败"或"IV 档位无读数"。每张真票多一次 10 年日线请求，
      留意 yfinance 有没有因此限流。
- [ ] **阈值校准**（1.0 / 1.25 / 指数 1.35 / 58%）。目前**没有回测**，依据强弱
      见 lesson.md 2026-09-25 "更正"一节。三条路：
      1. SPX 的 1 年期 IV 指数做指数代理回测（已做，见下）；
      2. 流水账前向验证：每季度按 `iv_band` 分组，看持有 6 个月后 LEAP 相对
         正股的表现（LEAP 不计胜率，比的是超额）；
      3. 付费的个股期权历史数据（ORATS / OptionMetrics / CBOE DataShop）。

### 指数代理回测结果（2026-09-25，`research/leap_iv_vix1y_backtest.py`）

**方法**：SPX 2007-01 ~ 2026-09，每月第一个交易日入场，共 237 次。
- 比值 = 1 年期 IV ÷ SPX 过去 10 年滚动 252 日实际波动的中位数，按指数口径分档。
- IV 用 CBOE VIX1Y。它是方差互换口径，含虚值 put，比平值 IV 高：9/24 VIX1Y 21.8%，而 SPX 1 年平值只有 17.6%。所以按今天的比例（×0.807）和差值（−4.2 点）两种方法校正成平值口径，两种结论一致。
- 检验一：IV 减去事后 1 年的实际波动，看 IV 有没有高估。
- 检验二：买 1 年期的 0.80δ call 和平值 call，持有 6 个月、用 VIX6M 定价平仓。"期权成本" = (期权盈亏 − 入场 delta × 指数盈亏) ÷ 权利金，也就是剥离方向后剩下的时间价值、vega 和 gamma。

**结果**（比例校正；相减校正的数字相近）：

| 比值区间 | n | 年份数 | IV − 事后实际波动 | 高估占比 | 0.8δ 成本 | 平值成本 | SPX 6 个月中位 |
|---|---|---|---|---|---|---|---|
| < 1.0 | 58 | 10 | +0.2 点 | 57% | +1.0% | −12.4% | +5.0% |
| 1.0–1.15 | 68 | 15 | −3.7 点 | 50% | +1.5% | −8.5% | +2.1% |
| 1.15–1.25 | 40 | 13 | +0.5 点 | 53% | −2.2% | −19.2% | +7.5% |
| 1.25–1.35 | 15 | 7 | +6.5 点 | 73% | −3.3% | −25.0% | +11.3% |
| 1.35–1.5 | 12 | 8 | +5.6 点 | 75% | +0.4% | −22.6% | +13.3% |
| > 1.5 | 44 | 8 | +6.7 点 | 84% | −0.6% | −19.1% | +10.3% |

**读法**（证据强度：中偏弱）：
1. **比值高时，IV 确实高估了未来波动。** 1.25 以下 IV ≈ 事后实际波动（±4 点），1.25 以上系统性高 5–7 点，高估的占 73–84%。这与 Dew-Becker 等 2017 一致：正常时期长期限 IV 没什么溢价。
2. **分界在 ~1.25，不是 1.35。** 指数单独用 1.35 的理由是 QQQ 一天的截面，这 20 年的检验不支持它。
3. **深度实值对 IV 贵不贵几乎不敏感。** 0.8δ 的期权成本在各区间都只有 −3% 到 +2%。**平值才怕贵**：比值 1.15 以上，平值多亏 10–15 个点的权利金。支持"A 可以平值，B/C 只做深度实值"。
4. **比值高的时期，事后指数涨得更多**（6 个月中位 +10–13%，1.15 以下只有 +2–5%）。高比值多出现在危机之后（2009–11、2020–22），紧接着就是反弹。C 档现在的动作是"改 spread 或等回落"：spread 封顶，等待则会错过这些反弹。**对指数来说，C 档更合适的动作是"只做深度实值、不碰平值"。**

**局限**：
- 只测了指数，而且 SPX 在样本内每次危机都修复了；个股可能不会。
- 1 年窗口互相重叠，237 次入场里大约只有 20 个独立年份。> 1.35 的样本集中在 6–8 段行情里，1.25–1.5 两个区间各只有十几个点。
- 平值校正用的是今天的 skew，历史上 skew 不是常数。
- 0.8δ 合约按指数整体的 IV 定价，没有加 skew。
- 股息按 1.8% 常数计算，利率用 ^IRX。

**待决定**：(a) 指数 C 线要不要从 1.35 改成 1.25；(b) C 档的动作要不要从"spread / 等回落"改成"只做深度实值"（技能和扫描器要一起改）。个股需要付费数据才能做同样的检验。

### 关闭条件

- 生产验证：连续 10 个交易日的报告里，出过的 LEAP 票都有档位读数、没有
  失败 note。满足后勾掉。
- 阈值校准：指数回测已给出方向（见上）；还需要流水账里至少 30 张带 `iv_band` 的
  LEAP 满 6 个月、各档都有样本。按结果调整阈值或维持，结论并入 lesson.md。

### 相关

`scanner.py` 的 `leap_iv_band()` / `attach_leap_iv_band()` / `mid_first_iv()`；
测试 `TestLeapIvBand`、`TestAttachLeapIvBand`、`TestLeapIvBandNotes`、`TestMidFirstIv`；
stock-analysis 技能的 `scripts/leap_iv_gauge.py`（同口径）。

---

## 4. 指数的 delta 带 vs 外在价值 ≤40% —— 等策略决定

**状态**: 规则冲突，未改。

### 背景

LEAP 的 IV 改为 mid 反解后，delta 算准了，同一个 delta 目标选出的行权价变浅
（QQQ 620C → 660C）。指数 delta 带 0.70–0.80 与"外在价值 ≤40%"本来就冲突 ——
笔记里早写过"两边都会逼到 ~0.82+"—— 以前靠偏低的 delta 碰巧过了 40% 门，现在
QQQ 0.75δ 的 660C 外在 49%，会带"深度不够"旗标出票；2029 到期的个股票
（NVDA 175C 44%）也常见。

### 选项

- (a) 指数 delta 带上调到 ~0.78–0.85；
- (b) 外在价值门对指数放宽到 ~50%；
- (c) 维持现状，只看旗标。

### 关闭条件

选定一项 → 改 `[settings]` 的 `leap_delta_index` 或 `leap_max_extrinsic_pct`，
同步 README 与 stock-analysis 笔记第七节。

---

## 5. CSP / 回踩 spread / atm_iv30 的 IV 口径 —— 先量偏差再决定

**状态**: 未动。这几处仍用 `contract_iv`（Yahoo 的 impliedVolatility 列优先）；
只有 LEAP 改成了 mid 反解。

### 背景

Yahoo 列对深度实值 LEAP 偏高 ~9 pts（lesson.md 2026-09-24）；短期限 OTM 上
9/4 实测差 ~2 pts，但从没系统量过。口径不一致，而 CSP 的 delta（选行权价）
和复盘的"Δ当前"都依赖这个 IV。

### 待办

- [ ] 用**收盘时点的 CBOE 报价**作基准（与收盘股价同一时点，见 lesson.md
      2026-09-25 "回放要用同一时点的报价"），量 Yahoo 列 vs mid 反解在三类
      合约上的偏差分布：CSP（2–4 周、0.10–0.15δ put）、回踩 spread（3–6 个月
      0.60/0.30δ call）、30 天平值。

### 关闭条件

偏差 < 1 pt 且没有方向性 → 维持现状，结论记进 lesson.md。否则按 LEAP 的方式
改成 mid 优先 —— 但先想清楚 9/4 那条教训：**mid 反解会引入对 spot 准确性的
依赖**，改之前确认收盘扫描的 spot 与报价同步（`daily_bar_stale` 门）。

---

## 附：第 1、2 项的共同点

都源自**同一个方法论问题** —— 用间接证据代替直接证据。

- 用"价格差"推断"数据陈旧" → 混进了借券成本；换成日期比对就干净了。
- 用"单点 RR 读数"推断"存在 call skew" → 混进了报价噪声；需要时间序列
  或外部源才能分开。

下次再想加"某某异常 = 某某结论"的判据时，先问一句：**有没有更直接的
证据可以拿？** 直接证据通常就在手边（这次 `lastTradeDate` 一直都在链
里），只是没想到去用。
