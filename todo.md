# todo.md — 未决项

记**还没验完**的东西，以及每项"怎么才算可以关掉"。已经踩过并定论的坑
去 [lesson.md](lesson.md)。

---

## 1. 陈旧日线硬拦 —— 已实现，等生产数据验证

**状态**: 代码已上（日期比对 + ⏸ 硬拦），但**只在盘后 `--force` 的人造
条件下验证过**，生产上一次都还没触发过。

### 背景

2026-09-04 盘后跑全量扫描，11 个标的亮了 10 个假 RR 倒挂旗标。根因是
yfinance 日线最新一根返回 NaN，scanner 退回前一日收盘当 spot，用昨天的
股价去反解今天的期权报价（详见 lesson.md）。

修了两层：

1. RR 的 IV/delta 改用 put-call parity 反解的 forward，陈旧 spot 不再
   传导到旗标；
2. 陈旧日线本身做成硬拦 —— 因为它同时污染收盘价、右侧状态判定和所有
   票据，不只是 RR。

### 判据为什么是日期比对而不是价差

一开始用的是"forward 反解现货 vs 日线收盘"的价差（`rr_spot_gap_warn`）。
问题：**难借券的高借券费会让 forward 合法地低于现货好几个百分点** ——
借券费在定价上等同于股息，`F = S·e^((r−q−b)T)`，`b` 一大 F 就掉下来。
价差判据分不开"数据陈旧"和"借券贵"，拿它硬拦会把 RKLB / SOFI / HOOD /
SPCX 这类高做空兴趣的标的**永久静音**，而且给出的理由（"日线陈旧"）
还是错的。

改成比对**日线 bar 的日期** vs **期权链 `max(lastTradeDate)` 的日期**：
借券费再贵也不会让两个日期对不上。单向判断 —— 只有期权比日线新才算陈旧，
反过来（期权好几天没成交）是流动性问题不是数据问题。

### 待验证

- [ ] **生产上会不会触发**。理论上不该 —— 定时任务不带 `--force`，
      `market_is_live()`（SPY 近 20 分钟有没有 1m bar）会先 exit 3。
      但那道检查证明的是"整个市场在交易"，**不证明"这只票的日线是今天
      的"**，残余风险就在这里。留意邮件里有没有出现 ⏸ 日线陈旧。
- [ ] **会不会误伤**。特别看难借券的那几只（RKLB / SOFI / HOOD /
      SPCX）：它们应该走"持有成本异常"那条 note，**不该**被硬拦。
- [ ] **盘中真实运行确认**。至今所有验证都是盘后 `--force` 造出来的，
      没有一次是在美股开盘时段跑的。

### 关闭条件

连续 10 个交易日的定时报告里，① 没有非预期的 ⏸ 日线陈旧；② 难借券标的
仍能正常出票。满足则删掉这一节，把结论并入 lesson.md。

### 相关

`scanner.py` 的 `daily_bar_stale()` / `blocked_ticket()`；测试
`TestDailyBarStale`、`TestBlockedTicket`。

---

## 2. RKLB / GLD 的 RR 倒挂旗标 —— 真伪未定

**状态**: forward 修复后，假旗标从 10 个降到 2 个。剩下这两个**看起来
是真的**，但没有独立确认过。

```
RKLB  80C IV 69.9% > 56P IV 66.5%   RR −3.4 pts
GLD  435C IV 25.9% > 392P IV 24.3%  RR −1.6 pts
```

### 为什么还不能下结论

- 两个读数都**压在 `rr_invert_min_pts = 1.0` 的噪声地板附近**，GLD 的
  −1.6 尤其近。地板本身是按"延迟报价有 ~1 pt run-to-run 漂移"定的。
- 数据是**盘后**的。盘后价差天然变宽，虽然新的 `_rr_mark` 会拒掉过宽的
  报价，但通过过滤的那些报价质量仍不如盘中。
- 单点观测，没有时间序列 —— 分不清"持续的 call skew"和"当天的噪声"。

两个都有先验上的合理性（RKLB 是高波动太空股，追涨常态；黄金本来就常有
上行需求推出的 call skew），但先验不等于验证。

### 待验证

- [ ] 盘中跑一次（布里斯班 23:30 之后），看这两个旗标还在不在、数值稳
      不稳。
- [ ] 连续几天看是否持续 —— 真 skew 会持续，噪声不会。
- [ ] 和外部源交叉核对（moomoo 的合约 IV，或 volchart.io 的 skew）。

### 关闭条件

盘中运行 + 连续 3 天都在 → 认定为真信号，记进 lesson.md 当作"旗标正常
工作"的基线样本。若盘中消失 → 说明 1.0 pt 的地板对盘后数据不够，需要
按数据时段分别定阈值。

---

## 附：两件事的共同点

都源自**同一个方法论问题** —— 用间接证据代替直接证据。

- 用"价格差"推断"数据陈旧" → 混进了借券成本；换成日期比对就干净了。
- 用"单点 RR 读数"推断"存在 call skew" → 混进了报价噪声；需要时间序列
  或外部源才能分开。

下次再想加"某某异常 = 某某结论"的判据时，先问一句：**有没有更直接的
证据可以拿？** 直接证据通常就在手边（这次 `lastTradeDate` 一直都在链
里），只是没想到去用。
