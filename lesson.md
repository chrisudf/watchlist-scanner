# lesson.md — 踩过的坑

按"症状 → 根因 → 教训"记。只记**当时判断错了或差点判断错**的，纯粹的
需求变更不记。

---

## 2026-09-04 · 假 RR 倒挂：10/11 个标的同时亮旗标

### 症状

盘后手动跑 `scanner.py --mode close --force`（全量、带期权链），11 个标的
里 **10 个**亮了 ⚠️ 25Δ RR 倒挂。读数离谱：

```
HOOD  140C IV 107.7% > 101P IV 32.4%   RR -75.3 pts
MSFT  535C IV  31.1% > 480P IV 19.3%   RR -11.8 pts
GLD   433C IV  30.3% > 388P IV 20.3%   RR -10.0 pts
```

### 第一个信号：基率不对

RR 旗标的设计是**例外才报告**。11 个里亮 10 个，这本身就是"指标坏了"的
证据，比任何单个读数是否合理都更强。**先看基率，再看个例** —— 如果一个
"例外报警"在多数标的上同时触发，先怀疑指标，不要逐个去解释为什么这个票
的 skew 真的倒了。

### 定位手法：两条腿各自反解 spot

关键判据不是"IV 看起来对不对"，而是**同一份数据的内部一致性**。

Yahoo 自己的 `impliedVolatility` 列显示 MSFT 是 0.258(call)/0.254(put)，
基本平的；我们 mid 反解出 0.311/0.193。差异形态是**call 抬高 + put 压低**，
而且**每个标的方向一致** —— 这不是噪声，是单一参数错位的签名。

于是反过来解：固定 Yahoo 的 IV，问"什么 spot 能让这个 mid 成立"：

| | scanner 用的 spot | call 腿反解 | put 腿反解 |
|---|---|---|---|
| MSFT | 496.82 | 507.00 | 509.72 |
| HOOD | 106.99 | **124.29** | **124.18** |
| GLD | 402.78 | 408.23 | 409.00 |

**两条腿独立反解出同一个 spot**（HOOD 两腿差 0.1%）—— 期权链内部完全
自洽，错的是外面喂进去的 spot。到这一步根因就锁死了，不需要再猜。

> 通用手法：怀疑派生量算错时，**固定其余参数、反解可疑的那个输入**。
> 如果多个独立的观测反解出同一个值，那个值就是真相，你手里的是错的。

### 根因

```
recent daily closes: 2026-09-01=103.51, 2026-09-02=106.99, 2026-09-03=nan
option lastTradeDate: 2026-09-03 19:58 UTC
```

yfinance 的**日线最新一根是 NaN**（9/3 收盘后 4.5 小时仍未落定），scanner
取最后一根非 NaN 的收盘当"今日收盘"，于是用 **9/2 的股价**去反解 **9/3 的
期权报价**。HOOD 当天从 106.99 涨到 ~124（+16%），错位最大，旗标也最夸张。

spot 偏低 → 同一个市场价反解时 call 必须调高 IV、put 必须调低 IV。
**这正是 RR 倒挂的定义形态** —— 一个纯粹的数据错位被完美伪装成了信号。

### 教训

**① 反解 IV 会把 spot 误差搬进结果，Yahoo 的 IV 列不会。**

patch 0006 把 RR 从 Yahoo 的 `impliedVolatility` 列改成 mid 反解，是为了
修另一个真 bug（两腿不同源：预算 IV 列 vs mid 反解差 ~2 pts，把 MSFT/GOOG
的正常 skew 翻成假倒挂）。那个修复是对的，但它**引入了一个新依赖**：结果
从此对 spot 准确性敏感，而 Yahoo 的 IV 列是用正确 spot 算的，原本免疫。

> 换实现修 bug 时，问一句：**新实现依赖了什么旧实现不依赖的东西？**
> 那个新依赖有没有断言保护？这里没有，于是换了个 failure mode 而已。

**② 相对量比绝对量脆弱得多。**

RR 是两腿 IV 的**差值**，绝对水平的共同误差会抵消，但**方向相反的误差会
翻倍**。spot 偏低对 call 和 put 的影响恰好反向，所以 2% 的 spot 误差
（MSFT）能造出 11.8 pts 的假 RR —— 远超 `rr_invert_min_pts=1.0` 的噪声
地板。噪声地板挡的是随机抖动，挡不住系统性偏置。

**③ 静默的陈旧数据比报错危险。**

报告照常渲染，价格印着 106.99，没有任何"这是昨天的价"的提示。如果不是
基率异常引起怀疑，这份报告会被当成真的用。`market_is_live()` 验的是
**整个市场在不在交易**（SPY 近 20 分钟有没有 1m bar），不验**单个标的的
日线是不是今天的** —— 存在盘中某只票日线缺失/滞后而全局检查照样通过的
缺口。

### 这次为什么没伤到生产

`--force` 绕过了 `market_is_live()`。定时任务不带 `--force`，盘后跑会
直接 exit 3。**护栏是有效的，是我手动拆的。**

> 推论：`--force` 的存在意义就是"我知道我在盘外跑"。那么盘外跑出来的
> 报告，**任何 IV 派生读数都不可信**，不只是 RR。下次手动验证优先用
> `--no-options`，或者干脆等盘中。

### 待办

per-ticker 新鲜度断言：日线 bar 的日期 vs 链上 `max(lastTradeDate)` 不
一致 → IV 派生读数（RR、iv30）标为不可用，而不是照算。数据已经在手
（`lastTradeDate` 就在链里），成本很低。归到 follow-up，不混进倒挂门控
矩阵那个 PR。

---

## 2026-09-04 · 工具链：heredoc 把 `\\` 吃成了 `\`

批量改代码时用 `python - <<'PYEOF'` 传脚本，脚本里的锚点字符串含 Python
行连接符。按 quoted heredoc 的语义 `\\` 应该原样透传，实际到 Python 手里
变成了单个 `\` —— 于是三引号字符串里的 `\` + 换行被当成**行连接**吃掉，
锚点匹配 0 次。

排查方法值得记：**逐行二分锚点**，打印每个前缀的 `count()`，一眼看出在
第几行断的，`repr()` 出来立刻看到换行没了。

教训：**多行代码编辑不要走 heredoc**。写成 `.py` 文件再执行，没有任何
中间层会动你的字符串。另外锚点脚本要**先全部匹配成功再落盘**（先在内存
里 replace，任一锚点 count != 1 就 `sys.exit`），否则会写出改了一半的
文件。这次就是靠这个约定，第一次失败时文件是干净的。

---

## 2026-09-05 · 守卫设在错误的量纲上（连错两次）

### 症状

第一个全自动交易日的尾盘报告里，15 只标的有 **6 只**亮 RR 倒挂旗标，而且
五只挤在 −1.1 ~ −1.9 —— 地板是 1.0。信号紧贴阈值成堆出现，这本身就是
"阈值不对"的征兆，不是市场真有那么多倒挂。

### 第一次错：门和被保护对象不在一个量纲

09-04 加的 parity forward 用了 5 档中位数，并配了一道门：候选 forward
的**极差 / 中位 > 0.8%** 就判无读数。看起来很稳妥。

实测（8 个标的，用 5 个候选各算一次 RR）：

| 标的 | RR 散布 (pts) | forward 极差 | 比值 |
|---|---|---|---|
| GOOG | 1.68 | 0.330% | 5.09 |
| SOFI | 0.63 | 0.127% | 4.96 |
| MSFT | 0.42 | 0.082% | 5.12 |
| GLD | 0.05 | 0.009% | 5.6 |

八个标的的比值全在 **4.79~5.6** —— 理论杠杆 "~5 vol pts / 1% forward
误差" 被实测坐实。于是那道 0.8% 的门允许 **0.8 × 5 = 4 pts** 的 RR 噪声，
是 1.0 pt 地板的**四倍**：它永远不会触发（实测最大的 GOOG 才 0.33%），
却放行了远超地板的抖动。

> **加守卫时，阈值要用被保护对象的单位表述。** 用代理量（forward 百分比）
> 卡一个下游量（RR 点数），中间那个杠杆一乘，门就废了。这和 lesson.md
> 上一条"用价差推断陈旧 vs 用日期比对"是同一个病。

### 第二次错：用非鲁棒统计量去守鲁棒估计

改成卡 RR 散布 —— 但我写的是 `max − min`。**极差对单个离群点最敏感**，
而中位数的全部意义就是抗离群点：等于用中位数灭掉坏档，再用极差把它请
回来。一条既有测试（"坏一档时中位数仍应给出读数"）当场变红，抓住了它。

再量一次，这次同时看截尾极差（去掉最高最低）：

```
GOOG  各候选 RR:  -1.96  -1.14  -0.95  -0.73  -0.29
      全极差 1.68   截尾极差 0.41
GLD   各候选 RR:  -1.88  -1.85  -1.85  -1.84  -1.83
      全极差 0.05   截尾极差 0.01
```

GOOG 不是"一个坏档"，是**平滑铺开**。而截尾极差所有标的都 ≤ 0.41 ——
换成截尾，门又形同虚设。数值阈值这条路两头堵死。

### 最终判据：卡判定，不卡数值

真正致命的不是散布多大，而是 GOOG 那五个候选里 **两个越过 1.0 地板、
三个没越过** —— "算不算倒挂"取决于你挑了哪个 forward，那不是市场事实。
GLD 五个全在 −1.85 附近、SOFI 全在 −1.2 以下，才是稳的。

所以门改成：**候选之间必须对"是否倒挂"给出一致判定，不一致就没有读数。**
不需要任何魔数阈值。散布仍随读数带进报告，供人自己判置信度。

> **通用形式：当一个连续量要被拿去过一个阈值做二值决策时，检验的对象
> 应该是"决策稳不稳"，而不是"这个量抖得厉害不厉害"。** 抖 3 pts 但全在
> 地板同一侧，结论是稳的；抖 1.7 pts 却横跨地板，结论就是掷硬币。

### 附带修掉的小问题

同一份报告里，驱动整个 regime 判定的那行写 `as of 2026-09-03`，而相邻的
VVIX 行写 `as of 2026-09-04 盘中` —— 两套口径。VIX 那条是故意只报结算日
（代码注释称之为 "the honest date"），盘中嫁接另用一个 `含盘中临时点`
标记表示。但读者看到的效果是：**最重要的那行看起来最旧**。已统一成
`as of 2026-09-04 盘中·延迟15min [结算 2026-09-03]`。

> 同一个概念在相邻两行用两套写法，比两行都用差写法更糟 —— 读者会以为
> 那是有意义的差别。

---

## 2026-09-08 · 看门狗在假日报 MISSED：两套"今天该不该有报告"的标准

### 症状

Brisbane 08:30 收到告警：

```
missing: open close (2026-09-07 ET) — cron 未跑成/发信失败/美股假日;
检查 /opt/watchlist-scanner/reports/run_2026-09-08.log
```

而日志里三次 fire 全是正常的：

```
US market not live (holiday/half-day/stale feed) — skip   ← 09:45 ET 开盘档
US market not live (holiday/half-day/stale feed) — skip   ← 15:45 ET 尾盘档
outside scan windows (Mon 16:45 ET) — skip
```

2026-09-07 是美国劳工节，SPY 那天没有任何日线。扫描器判断正确、跳过正确、
exit 3 正确。**每一步都对，还是响了。**

### 根因

`market_is_live()` 的 docstring 写着"不需要假日日历就能同时抓住周末、假日、
半日市" —— 它做到了。但 `run_scan.sh` 的看门狗压根没调它，自己另写了一套：

```bash
if [ "$et_dow" -le 5 ]; then   # 只排除周末
```

于是系统里有**两套"今天该不该有报告"的标准**：扫描器那套懂假日，看门狗那套
只懂星期几。两边一岔，必然误报。

这不是"忘了加假日日历"，而是**守卫和被守卫者对"正常"的定义不一致**。守卫的
判据必须从被守卫者那里取，不能自己另立一套 —— 哪怕自己那套看起来更简单。

### 半日市：同一个洞的另一半

顺着查下去，11-27（感恩节次日，13:00 ET 收盘）会以另一种形式再响：开盘扫描
正常出报告，15:45 的尾盘扫描无盘可扫、正确跳过，看门狗报 `missing: close`。
所以判定不能是二值的"开没开市"，得是**"这天该有哪几份报告"** —— 休市 0 份、
半日市 1 份、整日 2 份。

### 第一版修法（错的）：把"问盘面"照搬了过来

`expected_report_modes(day)` 收口成唯一事实来源、看门狗只消费它的结论 ——
这一步是对的。错的是判据：我沿用了 `market_is_live()` 那套"问盘面、不查
日历"，取 `day` 往前一周的 SPY 1m bar，当天没有 bar 就判休市。

PR #12 的评审一句话点破：**普通交易日 Yahoo 抽风时，当天同样没有 bar。**
那天会发生什么？扫描器的 `market_is_live()` 看到陈旧报价 → 两次扫描全部
skip；看门狗的判定看到同一份空数据 → 判"休市" → 跟着闭嘴。**整天静默丢报，
而这正是看门狗存在的唯一意义。**

### 真正的教训：同一个信号，两个方向上的安全失败不一样

"那天没有 bar"这句话，在两处回答的是不同的问题：

| | 问的是 | 数据缺失时的安全答案 |
|---|---|---|
| `market_is_live()` | 现在能不能扫？ | **不扫**（最多少一份报告） |
| 看门狗 | 那天本该有没有？ | **照常报警**（沉默才是事故） |

同一份证据，安全方向相反。我复用了那个"优雅"的判据，却没有跟着复用它成立
的前提 —— 那个前提是"猜错只损失一次扫描"，而在看门狗这边猜错损失的是整个
告警。

> **复用一个判据之前，先问它原来的失败方向是什么、搬到新位置上还安不安全。**
> 判据准不准是次要的；判据错的时候往哪边倒，才决定它能不能放在这个位置。

再往下一层：**缺席不构成休市的证据**，除非你另有一份"哪些天本该有数据"的
独立预期。"休市"和"数据源坏了"在盘面上本质不可分 —— 这里躲不掉日历。

### 第二版修法：日历定沉默，盘面只有否决权

- **日历说了算**：`NYSE_HOLIDAYS` / `NYSE_HALF_DAYS` 显式列到 2027 年底，
  超出覆盖范围一律按整日交易算。**表过期只会让告警变吵，不会变哑。**
- **盘面只剩一个方向的权力**：日历说"该沉默"而盘面显示当天确实在交易时，
  否决这次沉默（表抄错了/临时休市判错了）。取不到盘面就不否决。

于是盘面故障的所有后果都收敛到"告警变多"，没有任何一条路径通向静默。
`last_session_bar()` 拉一整周而非只拉当天的设计仍然保留：整周为空 = 数据源
坏了、raise —— 这样连否决权自己都不会误判。

半日市的表**宁缺勿滥**：多列一天会让真丢的 close 报告静默，少列一天只是
误报一次。同一条原则的又一次应用。

日历表用 SPY 2026 全年实盘日线反查过（170 个交易日零不一致），两个年度的
条目也与节假日规则推导逐条对上。

### 顺带：同一个 bug 在仓库里有两份

评审还翻出根目录那份 macOS 的 `run_scan.sh`（launchd 用）里躺着一模一样的
`et_dow <= 5`。**"收口成唯一事实来源"这句话，只有把所有调用点都接过去才
成立** —— 我改完 droplet 那份就宣布收口了，而仓库里还留着另一份反例。

### 附带：告警让你去查一个它自己不写的日志

邮件正文写"检查 …/run_2026-09-08.log"，而该文件的 mtime 停在最后一次 cron
fire —— **看门狗一个字都没往里写过**。`notify()` 只在发信有输出时才落盘，
成功路径完全静默。按提示打开日志的人看到的是别的 fire 的记录，看不到看门狗
凭什么报警。现在每次判定都留一行。

> 告警指向的位置必须有告警自己的痕迹，否则那句"检查 X"是在误导排查的人。

---

## 2026-09-24 · LEAP IV 档位上线当天的两处口径修正

背景：LEAP 的 IV 判断从"自建 IVP>60"（30 天口径）换成"平值 IV 在自身 10 年
实际波动里的分位"（PR #18）。用当天真实行情出演示报告时，暴露出下面两个问题。
两处修完都在报告里留了标注 —— **以后重构时，这两条最容易被当成"口径不一致"
顺手统一掉**，动之前先读这一节。

### 一、QQQ 被升档规则推成 C 档

#### 症状

QQQ 的 Jan'28 平值 IV 25%，在自身历史里是 71 分位（B 档），却被判成 C：
IV 超过了近两年实际波动（22%）的 1.15 倍，触发了升档。

#### 根因

1.15 倍的升档线是按个股想的。指数的 IV 相对实际波动天然溢价更高：成分股涨跌
不同步，实际波动被分散压低；期权卖方却要为"一起跌"的相关性风险收钱（相关性
风险溢价）。所以指数的 IV/实际波动常态就在 1.15–1.25，按个股那条线，QQQ 会
经常被判成贵。

#### 修法

新增 `leap_iv_bump_index = 1.25`，只对 `kind = "index"` 生效。**（9/25 已改：升档不再单设倍数，指数 C 线 1.35，见 2026-09-25 一节。）**GLD、DRAM 这类
etf 不是分散化的股票指数，仍走个股线。报告的档位 note 里写出"指数升档线 1.25 倍"。

1.25 是判断值，没有校准过。流水账记了 `iv_band` / `iv_pctile`，攒够指数票的
样本再看。

#### 教训

> 一条按比例定的规则要跨资产类别用之前，先问：这个比例的**正常值**在不同
> 资产上一样吗？不一样，那就不是一条规则，是两条。

### 二、深度实值 LEAP 的"合约 IV"系统性偏高约 9 个点

#### 症状

演示报告里 NVDA 2028-01 170C 写着"合约 IV 52%"，同一张票的平值 IV 才 39%；
CBOE 上这张 9/22 是 40.5%。

当天盘中逐张核对（Yahoo 的 `impliedVolatility` 列 vs 从 bid/ask mid 反解）：

| 合约 (2028-01-21) | Yahoo 列 | mid 反解 |
|---|---|---|
| NVDA 170C | 51.7% | 42.0% |
| AMZN 190C | 50.2% | 41.3% |
| GOOG 260C | 49.0% | 40.1% |
| QQQ 620C | 37.6% | 29.1% |
| IONQ 30C | 88.1% | 82.9% |

方向一致、幅度相近 —— 和 9/4 那次一样，是单一口径错位的签名，不是噪声。

#### 根因

`contract_iv` 优先用 Yahoo 列，只要落在 0.01–5 之间就用。9/4 已经知道 Yahoo
列和 mid 反解在短期限 OTM 上差 ~2 pts；到了深度实值，时间价值只占权利金的两三成，
同样的定价差被放大到 ~9 pts。

**不只是显示问题**：delta 是拿这个 IV 算的。IV 偏高 → delta 偏低 → 同一个
delta 目标会选到更深的行权价。review.py 算"Δ当前"用的也是流水账里的这个 IV。

#### 修法

新增 `mid_first_iv`：先 mid 反解，反解不出才退回 Yahoo 列，并记下来源 `iv_src`。
LEAP 票的合约 IV 和平值 IV 档位都改用它；CSP、回踩 spread、`atm_iv30` 暂不动。

报告标注：
- 票上的合约 IV 后面带 `(mid反解)` 或 `(⚠Yahoo列)`；退回 Yahoo 时另加一条 note。
- 平值 IV 有腿用了 Yahoo 列时，档位 note 写"(含 Yahoo 列)"。
- 标注紧跟在"合约 IV N%"之后，review.py 的 `CIV_RE` 只认前半段，不受影响。

流水账新增 `iv_src`。**`iv` 列的语义从 9/24 起变了**：之前的行是 Yahoo 列口径
（深度实值偏高），之后看 `iv_src`；旧行没有这个字段。

#### 连带影响：选出来的行权价变浅了

IV 偏高时 delta 被算低，同一个 delta 目标会落到更深的行权价上。换成 mid 反解后
delta 回到真实值，选出的合约相应变浅：同一时刻 NVDA 从 170C 变成 175C，QQQ 从
620C 变成 660C。

这也让一个原本被掩盖的冲突浮了出来：指数的 delta 带是 0.70–0.80，而"外在价值
≤ 40%"这条过滤要到 ~0.82 以上才满足（笔记里早就写过"两边都会逼到 ~0.82+"）。
以前 QQQ 靠偏低的 delta 选到更深的合约，碰巧过了 40%；现在 0.75δ 的 660C 外在
价值 49%，会带着"深度不够"的旗标出票。这是既有规则之间的矛盾，这次没有改 ——
要改的是 delta 带或 40% 门，属于策略决定，不是 bug。

> 修掉一个输入的系统性偏差，下游所有"碰巧通过"的门都会重新洗牌。修完要看的
> 不只是那个数对了没有，还有哪些结论跟着变了。

#### 反解失败的典型

有股息的深度实值。KO 2028-01 65C 的 mid 26.82，低于不计股息的 BS 下界
（S − K·e^(−rT) = 27.65），任何 IV 都解不出来。这时退回 Yahoo 列（32.4%），
票上标 ⚠。

#### 新依赖（呼应 9/4 教训①）

换实现时要问"新实现依赖了什么旧实现不依赖的东西"。mid 反解依赖 **spot 准确**：
深度实值每 1% 的 spot 误差约等于 2.7 个 vol 点（NVDA 170C 实测：spot +1%，
42.0% 变 39.3%）。

保护已经在：LEAP 票被 `daily_bar_stale` 门拦着，日线落后于期权链就不出票，所以
生产路径上 spot 与报价同步。盘外 `--force` 跑出来的读数照旧不可信（9/4 推论）。
另一个隐含假设是 `RATE = 0.04`：深度实值的价格大头是 K·e^(−rT) 这一项，对利率
假设同样敏感。

#### 教训

> "看起来合理"的范围检查（0.01 < iv < 5）挡不住系统性偏差。判断一个数据列能
> 不能用，要拿独立来源比一次（这里是 CBOE 和自己反解），而不是看它落没落在
> 合理区间。

#### 待办

CSP、回踩 spread、`atm_iv30` 仍是 Yahoo 列优先。短期限 OTM 的偏差小（~2 pts），
但口径不一致。要统一时先量偏差再改，别顺手 —— 9/4 的 RR 就是"顺手换实现"
换出了新的 failure mode。

---

## 2026-09-25 · 分位法把正常的 IV 溢价判成"贵"：回放 8 张历史 LEAP

### 症状

PR #18 第一版按"平值 IV 在自身 10 年滚动实际波动里的分位"分档（< 50 / 50–80 /
> 80）。拿过去 8 张 LEAP 推荐回放，新代码会改判其中 6 张（4 张 C、2 张 D）。

一个"例外才报警"的门对大多数票都亮了 —— 9/4 那条教训：**先怀疑指标，再解释个例**。

### 定位：看 IV 比实际波动中位数高了多少

| 推荐 | 分位 | IV ÷ 实际波动中位数 |
|---|---|---|
| TSM 2028 350C (9/8) | 81 | 1.15 |
| ISRG 2029 350C (9/21) | 99 | 1.14 |
| TSM 2029 340C (9/21) | 94 | 1.14 |

IV 只比自身实际波动的中位数高 14–15%，大约就是正常的波动率风险溢价，分位却有
81–99。同一只 TSM、同样 ~42% 的 IV，2028 到期 81 分位，2029 到期 94 分位。

### 根因

分位对分布的**宽度**敏感。窗口越长，滚动实际波动越向中位数收拢，分布越窄；
窄分布里一点正常溢价就排到很高的分位。第一版设阈值时假设"正常 IV 落在 60–70
分位"，数据否定了这个假设：今天多数大票的常态在 80–95 分位。

### 修法

改成**比值**：平值 IV ÷ 自身滚动实际波动的中位数。A < 1.0 / B 1.0–1.25 / C > 1.25；
58% 绝对门不变。中位数基本不随窗口长度变（合成数据：486 天 0.4042、851 天 0.4040），
比值也就不变。

改的过程中又发现三个坑，一起修了：

**① 升档线把 C 线架空了。** 第一版升档是"IV > 1.15 × max(近1年, 近2年实际波动)"。
换成比值后 C 线是 1.25，而近期波动 ≈ 长期的稳定标的，1.15 倍就先被升档成 C ——
1.25 的阈值被悄悄覆盖。现在升档不再单设倍数，直接用同一条 C 线。指数 C 线 1.35
（沿用指数比个股多 0.10 的相关性溢价），9/24 那条"指数升档线 1.25"随之作废。

**② 贴线翻档。** 9/25 实测 SOFI 平值 IV 58.5%，刚过 58% 绝对门，而它的比值 0.94
本来是 A；ISRG 比值 1.25 正压 B/C 线。延迟报价的抖动足以让它们隔天换档。现在比值
离分界 < 0.03、或 IV 离绝对门 < 2 个点时，note 写"读数噪声就能翻档"。只标注，不改档。

**③ "长期中位数过时"的提示条件太严。** GLD 近 1 年实际波动 30%、近 2 年 24%，
平值 IV 24.3% 夹在中间；10 年中位数只有 14%（黄金波动近两年翻倍），比值 1.69 → C。
原条件要求 IV 同时低于近 1 年和近 2 年，不报。改成低于较高者即报，并写出 IV 只有
近期较高者的几倍。仍然不自动降档 —— 买方默认保守。

### 重测

8 张历史推荐（推荐当日的平值 IV 没有记录，用"当时合约 mid 反解 IV − 今天同一
到期、同等价内程度的 skew"估算，两次估算间差 1–5 个点，边界票不确定）：

| 推荐 | 比值 | 档位 |
|---|---|---|
| NVDA 170C (8/9) | 0.84 | A |
| AAPL 260C (9/11) | 0.98 | A（贴 1.0 线） |
| ISRG 2029 (9/21) | 1.14–1.23 | B |
| TSM 2029 (9/21) | 1.12–1.14 | B |
| TSM 350C (9/8) | 1.15–1.28 | B/C 边界 |
| GLD 315C (8/9) | 1.55–1.81 | C（长期中位数过时） |
| COHR 240C (9/18) | IV 69–73% | D |
| RKLB 2029 (9/22) | IV 76–78% | D |

全 watchlist 15 只（今天盘中）：A 3 / B 6 / C 2 / D 4。"贵"只占少数，基率正常。

### 顺带确认：平值两腿平均对 spot 误差几乎免疫

spot 偏 1% 时（S=225、16 个月、σ=40%）：单看 call 腿 IV 错 1.5 个点，深度实值
175C 错 2.7 个点，而平值 call/put 平均只错 0.3 个点 —— 两腿的误差方向相反，平均时
抵掉了。所以**档位读数稳，票上那张深度实值合约的 IV 不稳**：盘中跑时股价是实时的、
期权报价延迟 15 分钟，TSM 360C 反解出 37%，比平值 40.7% 还低，就是这个错位。
生产的收盘扫描两者同步，不受影响。

### 这个比值法本身的坑（记下来，重构时别忘了）

- **衡量的是"相对自己的历史贵不贵"，不是"市场定错了价"。** 基本面真变了（COHR
  型），高比值可以是合理的，但买方照样要付这个价。
- **长期中位数会过时**，两个方向都会：波动下移（NVDA 十年前体量小）会把 IV 显得
  便宜，靠"升档"兜；波动上移（GLD）会把 IV 显得贵，只提示不降档。
- **个股和指数的正常溢价不一样**（指数 C 线高 0.10），其他资产类别（商品 ETF、
  杠杆 ETF）没有单独校准，目前走个股线。
- **用中位数不用均值**：抗 2020 年那种极端窗口，代价是中位数比均值低、比值偏严一点。
- **1.0 / 1.25 / 1.35 都是判断值**，没有历史 LEAP IV 数据可回测。流水账记了
  `iv_band` / `iv_ratio`，按档位看后续表现再校准。
- **历史短的票**（< 5 年）中位数只代表一个 regime，note 会提示"分位仅供参考"。

### 教训

> 定阈值前先用真实截面看一眼基率。"正常落在 60–70 分位"是拍出来的，拿 15 只票
> 算一遍就知道不对 —— 这一步比任何理论推导都便宜。

> 两条规则叠在一起时，检查它们的阈值是不是互相架空。升档本来是兜底，结果比主门
> 还严，主门的阈值就成了摆设。
